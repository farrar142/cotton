import { client } from './client';
import { Misc } from './misc';
import { Posts } from './posts';
import { Auth, Users } from './users';

const API = {
  client,
  Auth,
  Users,
  Misc,
  Posts,
};

export default API;
